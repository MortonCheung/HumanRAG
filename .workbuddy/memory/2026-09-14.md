# 2026-09-14 HumanRAG V11 比赛最终收口（第 47–61 章）

接手前一个 agent 的进度，继续完成 V11 比赛最终收口施工。分支 `feat/v11-humanrag-competition-final`，基线 HEAD `963070b`（未合并/未推送/未部署）。

## 本轮完成

- **第 48 章** Opening → Universe 时序图：`docs/architecture/v11-final/opening-universe.sequence.json/html`，9 参与者，validate/deliver 均 9/9 showcase。
  - 踩坑：`column_fit: spread` 下参与者框宽由 viewBox 宽度决定。viewBox 1120 + 9 参与者 → 框内宽 79px，sublabel `SpatialExperienceShell` 需 80px 报错；同时 1120 触发 1440×900 可读性失败（scale 0.83 → 7px 字体投影 5.81 < 6px）。可行窗口 ≈ **[1052, 1085]**，最终取 **1060**。
  - 结论：该模板下 9 参与者已是上限；10 参与者无法同时满足布局与可读性。
- **第 49 章** 目标提取时序图：`goal-extraction.sequence.json/html`，9 参与者（CameraController 并入卡片，因宽度上限），一次通过。
- **第 50 章** Architecture Delta：`delta-runtime.html` + `delta-runtime.receipt.json`，compare 28/28，base/head composition pass。
  - 坑：before 快照声明 source evidence → 必须传 `--repo-root .`，否则报 `repository-evidence/root-required`。
- **第 57 章** 新增单相机所有者不变式 E2E：`e2e/spatial-stage.spec.ts`「同一时刻只有一个相机所有者」——Universe 只有 `data-spatial-camera`，Library 只有 `data-preview-camera`，互斥且共享同一 Canvas。
- **第 59 章门禁全绿**：typecheck ✅；单测 43 文件 / 222 项 ✅；build ✅（spatial-runtime 1,091.51 kB / gzip 302.09 kB）；E2E 46 项 ✅。
- **第 61 章** 最终报告：`docs/humanrag-v11-competition-final-report.md`。

## 修掉的两个 E2E 失败

- `e2e/goal-extraction-camera.spec.ts`、`e2e/spatial-interaction.spec.ts` 用了 `clickPageAction` 但没 import。
- 第 30 章把次要操作收进 `...`（`hidden={!actionsOpen}`）后，`视图复位` 面板会被收起，旧的 `expect(overview).toBeVisible()` 不再成立 → 改为点击后用 `expectPageAction` 复验可达。

## 重要环境坑（务必记住）

**Edit 工具在本仓库对 `docs/`、`e2e/` 下的文件存在静默不落盘的情况**：报告 success 但磁盘内容未变（`grep`/`od` 可证实），且会出现同一文件内部分修改生效、部分不生效。**改用 Read + Write 整文件覆盖**才可靠；验证一律用 `node -e` 读文件或 `od -c`，不要只信工具回执。

## 仍未覆盖

- Windows / Safari / 真机触屏未实机验收；本机 Chromium 为软件渲染（SwiftShader），不能证明真实 GPU 帧时间。
- `lottie-web` 上游 `eval` 告警未修。
- 分支未合并/未推送/未部署。
