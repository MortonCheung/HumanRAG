# iTeach 项目执行规则

默认使用简体中文。先理解完整用户流程和现有实现，再制定计划并修改代码。

## 产品真相

- iTeach 是由知识树组织学习范围的大学刷题与定向教学产品；三维空间负责导航，不是产品本身。
- 层级固定为：知识库（专业/领域）→ 知识树（方向）→ 知识点（最小教学与出题单位）。
- 当前只实现计算机知识库；未来知识库复用同一数据模型、页面和工作区，仅替换内容。
- 题目和教学内容绑定知识点；知识树题库与知识库综合题库只是知识点题库的聚合，不复制数据。
- 全局导航不提供独立“教学”“刷题”首页。入口只有：Universe 具体节点直达，或知识库→知识树→学习/题库。
- Universe 与知识库进入同一知识点时必须复用同一 LearningWorkspace / PracticeWorkspace。
- 单知识树默认是使用状态；只有点击“编辑”后才加载结构、教学、题库和设置编辑工具。

## 体验边界

- 一个页面只负责一个任务；一个区域最多一个主操作；导航与操作分离。
- 不在不同页面重复同一功能，不为不同入口复制页面。
- 动画必须保持对象身份和来源—过程—去向，服务信息表达；用户操作时停止无关空闲动画。
- 使用克制的深色界面、稳定控件位置和充足留白；避免卡片墙、AI 渐变和装饰性粒子。
- 保留键盘操作、焦点、低动态模式、移动端适配和 Windows 性能降级。

## Ponytail 最小实现阶梯

来源：`iTeach-design-references/ponytail`（MIT）。每次编码按顺序停在第一个可行层级：

1. 这项功能真的需要存在吗？不需要就删除或跳过。
2. 代码库里已有实现吗？复用并修正共享根因。
3. 标准库或浏览器原生能力能完成吗？优先使用。
4. 已安装依赖能完成吗？不要为少量代码增加依赖。
5. 只写满足当前验收标准的最小正确实现，不为“以后可能需要”搭脚手架。

删除优先于新增，复用优先于重写，根因修复优先于路径补丁。不能简化掉数据校验、防止数据丢失的错误处理、安全、无障碍和明确要求。非平凡逻辑至少保留一个能证明行为的自动化检查。
